{CUSTOM_TOP}/admin/config/AKLOAD
- This is where the framework script utility will place AKLoad output files by default, and also where it expects to find them by default.
- This can be overridden by specifying the DATA_STAGING_DIR variable runtime.

- Library using it: {CUSTOM_TOP}/bin/xxcu_akutil.sh


