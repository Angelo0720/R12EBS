{CUSTOM_TOP}/admin/config/XDOLOAD
- This is where the framework script utility will place the XDOLoad output files by default, and also where it expects to find them by default.
- This can be overridden by specifying the DATA_STAGING_DIR variable runtime.

- Library using it: {CUSTOM_TOP}/bin/xxcu_xdoutil.sh

